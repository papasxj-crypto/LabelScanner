<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.meinook.labelscanner">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <!-- ALL PERMISSIONS GO HERE -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- HARDWARE FEATURES -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabelScanner">

        <!-- 1. MAIN ACTIVITY (Now handles both launcher and incoming share intents) [1.1.1] -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Shared Intent Filter: Targets web browser share sheets [1.1.1] -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 2. SCANNER ACTIVITY -->
        <activity
            android:name=".ScannerActivity"
            android:exported="false" />

        <!-- 3. OTHER ACTIVITIES -->
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- Declare Paywall Activity -->
        <activity
            android:name=".PaywallActivity"
            android:theme="@style/Theme.LabelScanner"
            android:exported="false" />

        <!-- Declare Lockout Activity -->
        <activity
            android:name=".LockoutActivity"
            android:theme="@style/Theme.LabelScanner"
            android:exported="false" />

        <!-- FILE PROVIDER -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="net.meinook.labelscanner.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>